{
    "configurations": [
    
        {
            "type": "node-terminal",
            "name": "START",
            "request": "launch",
            "command": "npm start",
            "cwd": "${workspaceFolder}"
        }
    ]
}